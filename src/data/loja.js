// Número reaproveitado do projeto +Ki Açaí (src/data/loja.js daquele projeto).
// Confirme se o pedido de bolos deve cair no mesmo WhatsApp ou em um número
// separado antes de publicar — se for separado, crie `whatsappBolo` aqui e
// ajuste `whatsappService.js` para escolher o número pela categoria do item.
export const loja = {
  nome: 'Sabor & Vontade',
  whatsapp: '5599985503978',
}
